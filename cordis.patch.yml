# @local/dsh-deepseek-console bundle patch
#
# This file is the `dsh.bundle.patch` layer of the plugin package: when the
# plugin is installed through the official CLI —
#
#   dsh plugin --profile web add @local/dsh-deepseek-console
#   (or: dsh plugin --profile web add link:/path/to/this/package)
#
# — the command reconciles `dsh.profile.bundles` against installed packages
# and, seeing this declaration, appends `@local/dsh-deepseek-console` to the
# bundle stack. The profile boot then merges THIS patch (a single `insert` of
# the plugin row) exactly like a manual cordis.patch.yml mount line. No
# profile file edits needed — one command installs and mounts.
#
# If the profile still carries a manual mount line for this plugin in its own
# cordis.patch.yml, remove it before switching to the bundle channel to avoid
# double-mounting (two host halves, two route registrations).
- insert:
    - id: deepseek-console
      name: '@local/dsh-deepseek-console'
